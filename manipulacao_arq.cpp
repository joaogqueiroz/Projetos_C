#include <stdio.h>
#include <stdlib.h>
int main (){
ftpr = fopen("teste.txt", "r"); //mesma pasta do arquivo .c
ptArquivo = fopen("teste.bin", "wb");
ponteiroArquivo = fopen("c:\tmp\teste.txt", "w+");
FILE *ponteiroArquivo;
ponteiroArquivo = fopen("teste.bin", "wb");
if (ponteiroArquivo != NULL)
printf("Arquivo aberto com sucesso!");
else
printf("Erro! Arquivo não foi criado!");
}
